# config.yml

backend:
  name: git-gateway
  branch: master
media_folder: 'assets/uploads'
collections:
  - name: 'sermons'
    label: 'Sermons'
    folder: '_sermons/'
    fields:
      - { name: Title }
        { name: Description, type: markdown }
